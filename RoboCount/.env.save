.venv/bin/python - <<'PY'
import os
from dotenv import load_dotenv
import psycopg

load_dotenv()

url = os.environ["DATABASE_URL"]

try:
    with psycopg.connect(url, connect_timeout=10) as conn:
        with conn.cursor() as cur:
            cur.execute("SELECT current_user, current_database(), version();")
            print(cur.fetchone())
    print("CONNESSIONE RIUSCITA")
except Exception as e:
    print(type(e).__name__)
    print(e)
PYDATABASE_URL=postgresql://postgres.dewlyfgaksgnuooaacny:mattiasimoneakirafalco@aws-1-eu-west-2.pooler.supabase.com:6543/postgres
SESSION_SECRET=mattiasimonestringasegretadisicurezza1234!
ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
MONITOR_SPESE_COOKIE_SECURE=false
VITE_API_BASE_URL=/api
